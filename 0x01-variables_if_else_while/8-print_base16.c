#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int a;
	char(upper);

	for (a = '0'; a <= '9'; a++)
		putchar(a);
	for (upper = 'A'; upper <= 'F'; upper ++)
		putchar(upper);
	putchar('\n');
	return (0);
}
